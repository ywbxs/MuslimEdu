import { ENDPOINTS } from '../config/api';
export type Program={id?:number;name:string;code?:string;degree_type?:string;status?:string};
export type Subject={id?:number;name:string;code?:string;credits?:number;department_id?:number|null;status?:string};
async function req(token:string,path:string,init:RequestInit={}){const base=(ENDPOINTS as any).academicCatalog||`${(ENDPOINTS as any).me.replace(/\/me$/,'')}/academic/catalog`;const r=await fetch(base+path,{...init,headers:{Accept:'application/json',Authorization:`Bearer ${token}`,...(init.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`Request failed (${r.status})`);return d}
export const academicCatalogApi={programs:(t:string)=>req(t,'/programs'),createProgram:(t:string,x:Program)=>req(t,'/programs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),deleteProgram:(t:string,id:number)=>req(t,`/programs/${id}`,{method:'DELETE'}),subjects:(t:string)=>req(t,'/subjects'),createSubject:(t:string,x:Subject)=>req(t,'/subjects',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),deleteSubject:(t:string,id:number)=>req(t,`/subjects/${id}`,{method:'DELETE'}),gradingSystems:(t:string)=>req(t,'/grading-systems'),gradeScales:(t:string)=>req(t,'/grade-scales')};
