import { ENDPOINTS } from '../config/api';
export type CompletionRule={id?:number;name:string;minimum_credits?:number;required_gpa?:number|null;enabled?:boolean};
async function req(token:string,path:string,init:RequestInit={}){const base=(ENDPOINTS as any).academicCompletion||`${(ENDPOINTS as any).me.replace(/\/me$/,'')}/academic/completion`;const r=await fetch(base+path,{...init,headers:{Accept:'application/json',Authorization:`Bearer ${token}`,...(init.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`Request failed (${r.status})`);return d}
export const completionApi={config:(t:string)=>req(t,'/config'),saveConfig:(t:string,x:CompletionRule)=>req(t,'/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),records:(t:string)=>req(t,'/records'),evaluate:(t:string,id:number)=>req(t,`/evaluate/${id}`),approve:(t:string,id:number,note?:string)=>req(t,`/approve/${id}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({note})})};
