import { ENDPOINTS } from '../config/api';
export type DocumentTemplate={id?:number;name:string;document_type?:string;language?:string;definition?:Record<string,unknown>;status?:string};
async function req(token:string,path:string,init:RequestInit={}){const base=(ENDPOINTS as any).academicDocuments||`${(ENDPOINTS as any).me.replace(/\/me$/,'')}/academic/documents`;const r=await fetch(base+path,{...init,headers:{Accept:'application/json',Authorization:`Bearer ${token}`,...(init.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`Request failed (${r.status})`);return d}
export const academicDocumentsApi={templates:(t:string)=>req(t,'/templates'),saveTemplate:(t:string,x:DocumentTemplate)=>req(t,'/templates',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),approveTemplate:(t:string,id:number)=>req(t,`/templates/${id}/approve`,{method:'POST'}),preview:(t:string,id:number,studentId:number)=>req(t,`/templates/${id}/preview`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({student_id:studentId})}),issue:(t:string,id:number,studentId:number)=>req(t,'/issuances',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({template_id:id,student_id:studentId})}),issuances:(t:string)=>req(t,'/issuances')};
