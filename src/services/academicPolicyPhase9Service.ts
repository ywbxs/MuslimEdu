import { ENDPOINTS } from '../config/api';
export type Policy={id?:number;name:string;policy_type?:string;description?:string;definition?:Record<string,unknown>;status?:string};
async function req(token:string,path:string,init:RequestInit={}){const base=(ENDPOINTS as any).academicPolicies||`${(ENDPOINTS as any).me.replace(/\/me$/,'')}/academic/policies`;const r=await fetch(base+path,{...init,headers:{Accept:'application/json',Authorization:`Bearer ${token}`,...(init.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`Request failed (${r.status})`);return d}
export const academicPolicyApi={list:(t:string)=>req(t,''),create:(t:string,x:Policy)=>req(t,'',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),update:(t:string,id:number,x:Partial<Policy>)=>req(t,`/${id}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),approve:(t:string,id:number)=>req(t,`/${id}/approve`,{method:'POST'}),evaluate:(t:string,id:number,studentId:number)=>req(t,`/${id}/evaluate`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({student_id:studentId})}),audit:(t:string)=>req(t,'/audit')};
