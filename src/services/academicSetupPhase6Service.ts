import { ENDPOINTS } from '../config/api';
export type SetupStatus={completed:boolean;institution_profile?:any;academic_years?:any[];terms?:any[]};
async function req(token:string,path:string,init:RequestInit={}){const base=(ENDPOINTS as any).academicSetup||`${(ENDPOINTS as any).me.replace(/\/me$/,'')}/academic/setup`;const r=await fetch(base+path,{...init,headers:{Accept:'application/json',Authorization:`Bearer ${token}`,...(init.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`Request failed (${r.status})`);return d}
export const academicSetupApi={status:(t:string)=>req(t,'/status'),saveProfile:(t:string,x:any)=>req(t,'/profile',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),saveYear:(t:string,x:any)=>req(t,'/years',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),saveTerm:(t:string,x:any)=>req(t,'/terms',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),complete:(t:string)=>req(t,'/complete',{method:'POST'})};
