import { ENDPOINTS } from '../config/api';
export type Campus={id?:number;name:string;name_ar?:string;code?:string;address?:string;phone?:string;email?:string;is_main?:boolean;status?:'active'|'inactive'};
export type GradeLevel={id?:number;name:string;name_ar?:string;code?:string;education_stage?:string;level_order?:number;status?:'active'|'inactive'};
async function req(token:string,path:string,init:RequestInit={}){const base=(ENDPOINTS as any).academicStructure||`${(ENDPOINTS as any).me.replace(/\/me$/,'')}/academic/structure`;const r=await fetch(base+path,{...init,headers:{Accept:'application/json',Authorization:`Bearer ${token}`,...(init.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`Request failed (${r.status})`);return d}
export const academicStructureApi={campuses:(t:string)=>req(t,'/campuses'),createCampus:(t:string,x:Campus)=>req(t,'/campuses',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),deleteCampus:(t:string,id:number)=>req(t,`/campuses/${id}`,{method:'DELETE'}),gradeLevels:(t:string)=>req(t,'/grade-levels'),createGradeLevel:(t:string,x:GradeLevel)=>req(t,'/grade-levels',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),deleteGradeLevel:(t:string,id:number)=>req(t,`/grade-levels/${id}`,{method:'DELETE'})};
