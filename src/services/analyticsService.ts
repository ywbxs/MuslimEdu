import AsyncStorage from '@react-native-async-storage/async-storage';import * as Config from '../../src/config/api';const c:any=Config as any,base=(c.API_BASE_URL||c.BASE_URL||c.API_URL||'').replace(/\/$/,'');async function p(x:string,b:any={}){let t=null;for(const k of ['token','auth_token','authToken','user_token']){t=await AsyncStorage.getItem(k);if(t)break}const r=await fetch(base+'/'+x,{method:'POST',headers:{Accept:'application/json','Content-Type':'application/json',...(t?{Authorization:'Bearer '+t}:{})},body:JSON.stringify(b)});const j=await r.json();if(!r.ok)throw new Error(j.message||'Request failed');return j}export default{dashboard:(b:any={})=>p('admin_academic_analytics_dashboard',b),student:(student_id:number)=>p('admin_academic_analytics_student',{student_id})};
