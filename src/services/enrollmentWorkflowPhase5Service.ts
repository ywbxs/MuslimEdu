import { ENDPOINTS } from '../config/api';
async function req(token:string,path:string,init:RequestInit={}){const base=(ENDPOINTS as any).enrollmentWorkflow||`${(ENDPOINTS as any).me.replace(/\/me$/,'')}/academic/enrollment-workflow`;const r=await fetch(base+path,{...init,headers:{Accept:'application/json',Authorization:`Bearer ${token}`,...(init.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`Request failed (${r.status})`);return d}
export const enrollmentWorkflowApi={stages:(t:string)=>req(t,'/stages'),records:(t:string)=>req(t,'/records'),start:(t:string,x:any)=>req(t,'/records/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),advance:(t:string,x:any)=>req(t,'/records/advance',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),withdraw:(t:string,x:any)=>req(t,'/records/withdraw',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),studentStatus:(t:string)=>req(t,'/student/status')};
