import AsyncStorage from '@react-native-async-storage/async-storage'; import * as Config from '../../src/config/api'; const c:any=Config as any,base=(c.API_BASE_URL||c.BASE_URL||c.API_URL||'').replace(/\/$/,''); async function post(p:string,b:any={}){let t=null;for(const k of ['token','auth_token','authToken','user_token']){t=await AsyncStorage.getItem(k);if(t)break}const r=await fetch(base+'/'+p,{method:'POST',headers:{Accept:'application/json','Content-Type':'application/json',...(t?{Authorization:'Bearer '+t}:{})},body:JSON.stringify(b)});const j=await r.json();if(!r.ok)throw new Error(j.message||'Request failed');return j}export const gradeService={versions:()=>post('admin_grade_versions'),calculate:(b:any)=>post('admin_grade_calculate',b),save:(b:any)=>post('admin_grade_save',b),submit:(id:number)=>post('admin_grade_submit',{id}),approve:(id:number)=>post('admin_grade_approve',{id}),list:(b:any={})=>post('admin_grade_list',b),audit:(id:number)=>post('admin_grade_audit',{id})};export default gradeService;
