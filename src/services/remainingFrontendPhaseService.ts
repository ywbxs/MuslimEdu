import { ENDPOINTS } from '../config/api';
async function req(token:string,path:string,init:RequestInit={}){const base=(ENDPOINTS as any).academicRemaining||`${(ENDPOINTS as any).me.replace(/\/me$/,'')}/academic`;const r=await fetch(base+path,{...init,headers:{Accept:'application/json',Authorization:`Bearer ${token}`,...(init.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`Request failed (${r.status})`);return d}
export const remainingApi={notifications:(t:string)=>req(t,'/notifications'),integrations:(t:string)=>req(t,'/integrations'),lifecycle:(t:string)=>req(t,'/lifecycle'),analytics:(t:string)=>req(t,'/analytics'),attendanceConfig:(t:string)=>req(t,'/attendance-config'),permissions:(t:string)=>req(t,'/permissions'),teacherProgress:(t:string)=>req(t,'/teacher/progress'),studentServices:(t:string)=>req(t,'/student/services')};
