import { ENDPOINTS } from '../config/api';
export type SubjectLoad={id?:number;student_id:number;subject_id:number;term_id?:number|null;status?:string;notes?:string};
async function req(token:string,path:string,init:RequestInit={}){const base=(ENDPOINTS as any).subjectLoading||`${(ENDPOINTS as any).me.replace(/\/me$/,'')}/academic/subject-loading`;const r=await fetch(base+path,{...init,headers:{Accept:'application/json',Authorization:`Bearer ${token}`,...(init.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`Request failed (${r.status})`);return d}
export const subjectLoadingApi={list:(t:string)=>req(t,''),create:(t:string,x:SubjectLoad)=>req(t,'',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),approve:(t:string,id:number)=>req(t,`/${id}/approve`,{method:'POST'}),reject:(t:string,id:number,reason:string)=>req(t,`/${id}/reject`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reason})}),mine:(t:string)=>req(t,'/mine')};
